class PostPreview {
    index: number
    id: number
    thumbnailSrc: string

    constructor(index: number, id: number, thumbnailSrc: string) {
        this.index = index
        this.id = id
        this.thumbnailSrc = thumbnailSrc
    }
}

export default PostPreview